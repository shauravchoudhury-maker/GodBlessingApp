# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.log

# Local scratch / build artifacts (none required to run the app)
*.tmp

# Local Claude Code / editor tooling config (machine-specific)
.claude/
